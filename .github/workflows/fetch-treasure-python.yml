name: Fetch Treasure (Python)

on:
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run Python treasure script
        env:
          TREASURE_KEY: ${{ secrets.TREASURE_KEY }}
        run: python fetch_treasure.py
